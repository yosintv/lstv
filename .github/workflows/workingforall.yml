name: Working Agent For All

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Libraries
        run: pip install curl_cffi pycountry

      - name: Build Website
        run: |
          echo "🔨 Running main.py..."
          python main.py
          echo "✅ Build complete - check 'Matches Loaded' count above"

      - name: FORCE index.html Update
        run: |
          # Add unique timestamp to ensure Git detects change
          TIMESTAMP=$(date -u +%Y%m%d%H%M%S)
          sed -i "s|||" index.html 2>/dev/null || echo "" >> index.html
          echo "✅ index.html timestamp updated: $TIMESTAMP"

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Explicitly include the pt/ folder along with other standard directories
          git add index.html sitemap.xml home/* match/* channel/* pt/* 2>/dev/null || git add .
          
          if git diff --staged --quiet; then
            echo "⚠️ No changes detected"
          else
            # Count includes the new pt/ folder to show total pages in the commit message
            TOTAL_PAGES=$(find home/ match/ channel/ pt/ -name '*.html' 2>/dev/null | wc -l | xargs)
            git commit -m "🤖 Update: index.html + $TOTAL_PAGES pages [skip ci]"
            git push origin main
            echo "✅ ALL FILES PUSHED including index.html and pt/ folder content"
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
